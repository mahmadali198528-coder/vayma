import { notFound } from "next/navigation";import { news } from "@/data/public";import type { Metadata } from "next";export function generateStaticParams(){return news.map(n=>({slug:n.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const item=news.find(n=>n.slug===slug);return {title:item?.title,description:item?.excerpt}}export default async function NewsDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=news.find(n=>n.slug===slug);if(!item)notFound();return <article className="container py-20"><p className="eyebrow">{item.category} · {item.publishedAt}</p><h1 className="title">{item.title}</h1><p className="lead">{item.content}</p></article>}
