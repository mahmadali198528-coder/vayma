import type { MetadataRoute } from "next";import { news } from "@/data/public";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL??"http://localhost:3000";return ["","/about","/education","/admission","/teachers","/schedule","/news","/events","/gallery","/contacts",...news.map(n=>`/news/${n.slug}`)].map(path=>({url:`${base}${path}`,lastModified:new Date()}))}
